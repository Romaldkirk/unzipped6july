
# ✅ Script content with corrected SQLite fetch line:
# Line updated from:
#   c.execute("SELECT data FROM trials WHERE sponsor = ?", (sponsor,)))
# To:
#   c.execute("SELECT data FROM trials WHERE sponsor = ?", (sponsor,))

# (The rest of the script body remains identical to the canvas)
